instances:
  instance[01-02]:
    provider:
      name: libvirt_provider
      driver: libvirt
    settings:
      args:
        - "{{ instance.name }}"
        - /var/lib/libvirt/images/{{ instance.name }}.qcow2
      kwargs:
        memory_size: 2048mib
        num_vcpus: 2
  instance03:
    plan: test_plan
  instance[04-05]:
    plan: test_plan